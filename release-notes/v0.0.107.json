{
  "$schema": "../scripts/release-notes-schema.json",
  "version": "0.0.107",
  "date": "2026-05-23",
  "headline": "`X-Robots-Tag: noindex, nofollow` on `/cart` — crawlers stop indexing the session-bound empty-state",
  "summary": "The guest `/cart` route ships an empty-cart render at the edge for visitors without a session cookie. Crawlers reaching the page were indexing it as if it were content; the empty-state snippet (\"Your cart is empty\") was appearing in SERPs as a stale search result for unrelated queries. The handler now sets `X-Robots-Tag: noindex, nofollow` on the response so well-behaved crawlers skip indexing. `robots.txt` already `Disallow`s the route — the response header is belt-and-suspenders.",
  "sections": [
    {
      "heading": "Changed",
      "items": [
        { "title": "`_edgeCartEmpty` sets `X-Robots-Tag: noindex, nofollow`", "body": "The handler now builds its own Response (instead of routing through `_html`) so the response carries the X-Robots-Tag header alongside the full security-header set + Link preload + minification. Per-route headers compose cleanly without changing `_html`'s signature for one outlier." }
      ]
    }
  ]
}
