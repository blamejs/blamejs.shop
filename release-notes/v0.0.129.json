{
  "$schema": "../scripts/release-notes-schema.json",
  "version": "0.0.129",
  "date": "2026-05-25",
  "headline": "Recently viewed — a signed-in customer's browse history at /account/recently-viewed",
  "summary": "Signed-in customers now have a browse history. Opening a product page records the view against the customer's account, and `/account/recently-viewed` lists those products newest-first as a grid, with a one-tap Clear history control. Recording is best-effort and never blocks the product page; archived products drop out of the grid; the page is login-gated like the rest of the account area.",
  "sections": [
    {
      "heading": "Added",
      "items": [
        { "title": "Recently viewed account page", "body": "`GET /account/recently-viewed` renders the customer's most-recently-opened products newest-first, reusing the standard product card (image, title, price, link to the PDP). A `POST /account/recently-viewed/clear` control wipes the history. Linked from the account dashboard." },
        { "title": "Server-side view recording", "body": "A signed-in customer's product-page visit records the view against their account — no client script. Recording is drop-silent: a write failure never breaks the product page. The history de-dupes per product and is capped per customer." }
      ]
    }
  ]
}
