{
  "$schema": "../scripts/release-notes-schema.json",
  "version": "0.0.14",
  "date": "2026-05-20",
  "headline": "Worker container cold-start retry — 3 attempts, exponential backoff",
  "summary": "Container cold-start can still surface `Failed to start container: The container is not running` when the underlying SDK's default 8-second port-wait window is exceeded under provisioning load. The single-retry-with-1.5s-backoff shipped in v0.0.9 wasn't always enough; this patch ups it to three retries with 2s / 4s / 8s backoff (~14 seconds total patience for the cold-start window).",
  "sections": [
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "`worker/index.js` cold-start retry — 3 attempts",
          "body": "`_forwardToContainer` now retries up to three times on `Failed to start container` / `no Container instance` 5xx responses with 2s / 4s / 8s exponential backoff. The first attempt is immediate; legitimate 5xx (non-cold-start body shapes) short-circuit so application errors still surface immediately. POST bodies buffer once on entry and re-serve from the ArrayBuffer on each retry; GET / HEAD have no body and clone cheaply."
        }
      ]
    },
    {
      "heading": "Changed",
      "items": [
        {
          "title": "Vendor refresh blamejs v0.11.36 → v0.11.37",
          "body": "Caught up to upstream."
        }
      ]
    }
  ]
}
