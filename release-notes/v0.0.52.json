{
  "$schema": "../scripts/release-notes-schema.json",
  "version": "0.0.52",
  "date": "2026-05-22",
  "headline": "Live deploy moves to the custom domain — README + wrangler config point at https://blamejs.shop",
  "summary": "The reference deploy now serves at `https://blamejs.shop` instead of the `*.workers.dev` subdomain. README header now reads 'Homepage: **https://blamejs.shop**' and the admin-API curl example uses a placeholder `your-shop.example.com` host since operators replace it with their own. `wrangler.toml#D1_BRIDGE_URL` switches to `https://blamejs.shop` so the container's externalDb adapter calls back through the canonical origin.",
  "sections": [
    {
      "heading": "Changed",
      "items": [
        {
          "title": "README header — 'Homepage' instead of 'Live demo', custom domain",
          "body": "`Homepage: **https://blamejs.shop**` replaces the previous `Live demo: **https://blamejs-shop.coocoo.workers.dev/**`. Operators evaluating the framework now see the canonical address. The admin-API curl example in the operator quick-start drops the placeholder `<your-worker>.workers.dev` host in favour of a `your-shop.example.com` placeholder with a comment pointing at the reference deploy."
        },
        {
          "title": "`wrangler.toml#D1_BRIDGE_URL` → `https://blamejs.shop`",
          "body": "The container's externalDb D1 adapter posts SQL to `<D1_BRIDGE_URL><D1_BRIDGE_PATH>` (the Worker's service-binding bridge endpoint). Updating the base URL routes those internal calls through the custom domain. Cloudflare resolves custom domains to the same Worker that serves `*.workers.dev`, so the bridge keeps working with no router change."
        }
      ]
    }
  ]
}
