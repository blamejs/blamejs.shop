{
  "$schema": "../scripts/release-notes-schema.json",
  "version": "0.2.6",
  "date": "2026-05-27",
  "headline": "Business hours — a public /hours page with live open/closed status, managed from the admin console",
  "summary": "Operators can publish their open hours. Define one or more schedules (for example a support line and a pickup counter) from a new Hours screen in the admin console — set a timezone and an open/close time per weekday. The public /hours page renders each schedule as a weekly grid with a live 'Open now' or 'Closed' status computed at request time in the schedule's own timezone, plus the next opening or closing transition. Link it from your navigation, footer, a policy page, or an announcement bar. Holidays and one-off date exceptions are supported through the JSON API and override the weekly base. Until a schedule is published, the page shows a brief 'not published yet' notice.",
  "sections": [
    {
      "heading": "Added",
      "items": [
        { "title": "Public /hours page", "body": "A server-rendered page listing every active schedule as a weekday grid (closed days marked), with a live open/closed pill and the next transition (\"Opens Monday at 09:00\" / \"Closes at 17:00\"). The status is timezone-aware and computed per request, so it isn't cached stale." },
        { "title": "Admin console: Hours", "body": "A new Hours screen (and matching JSON API) to create a schedule with a timezone and per-weekday open/close times (a blank day is closed), list schedules, and archive ones no longer in use. Holiday closures and one-off date exceptions — which override the weekly base — are available through the API. The screen appears only when the business-hours primitive is wired." }
      ]
    }
  ]
}
