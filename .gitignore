# Node
node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log
.npm/

# Test output (gitignored — smoke writes to .test-output/smoke.log)
.test-output/

# Build / dist
dist/
build/
*.tgz

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log

# Local-only state
.env
.env.*
!.env.example

# Local maintainer state (not for the repo)
keys/release-pqc-priv.json
keys/*-priv.*

# Intermediate / scratch
.scratch/
.scratch-*/
.extract-staging/

# Wrangler / Cloudflare Workers local state (dev cache, deploy
# fingerprints) — never committed.
.wrangler/

# Local visual reference material — the two ecommerce-template zips
# are reference for the future theme primitive, not shipped code.
.template/

# Visual-verification artifacts — Playwright drops PNG snapshots at
# the repo root (live-*.png, e2e-*.png) and under `.playwright-mcp/`
# while testing the storefront UI; never part of the release surface.
/*.png
.playwright-mcp/
